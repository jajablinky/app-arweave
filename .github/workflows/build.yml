name: Build

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - master
      - develop
  pull_request:

jobs:
  build:
    name: Build Application
    uses: LedgerHQ/ledger-app-workflows/.github/workflows/reusable_build.yml@v1
    secrets: inherit
